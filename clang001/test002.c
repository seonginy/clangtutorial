#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int i = 5; //정수형 변수
    float f = 10.5F; //부동소수점형 변수
    double d = 20.123;//부동소수점형 변수
    
    printf("%d, %f, %lf \n", i, f, d);

    return 0;
}
